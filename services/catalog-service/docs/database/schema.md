# Banco

Entidades canônicas e migrations flyway v1–v13 no schema catalog. O PostgreSQL é compartilhado atualmente; não existe um banco independente para ingestão. Diagramas e SQL completos são mantidos no handbook database. Não armazenar EPUB/TXT completo em colunas.
