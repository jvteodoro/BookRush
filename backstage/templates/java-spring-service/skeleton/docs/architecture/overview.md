# Overview

Service: ${{ values.name }}. ${{ values.description }}

Spring Boot Java 21, Maven and containerized runtime. The skeleton follows the existing platform Boot baseline; upgrade it independently with tests. Domain architecture is Not applicable yet because no business functionality is generated.
