# Contributing

Service: ${{ values.name }}. ${{ values.description }}

Not applicable yet: this generated skeleton has no domain model, persistence mappings, event handlers or operational workload. Replace this explanation with the actual design when implementing this concern; do not copy another service’s architecture.
