apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: jenkins
  description: Pipeline Jenkins de build, integração e deploy.
  annotations:
    backstage.io/techdocs-ref: dir:./jenkins-docs
spec:
  type: service
  lifecycle: production
  owner: group:default/platform-engineering
  system: platform-infrastructure
